# Source structure

This directory will contain the text-first School of the Ancients app implementation. Product code should keep canonical learning state in backend/app services and use the server-side AI gateway for model calls.
